spring:
  datasource:
    url: jdbc:h2:mem:techfiyr;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false
  docker:
    compose:
      enabled: false
  thymeleaf:
    cache: false

app:
  upload-dir: ${java.io.tmpdir}/techfiyr-test-uploads
  bootstrap:
    admin-username: admin
    admin-password: ChangeMe123!
    employee-username: employee
    employee-password: Employee123!
